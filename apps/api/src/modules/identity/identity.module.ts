import { Module } from '@nestjs/common';

/**
 * Identity module — global person + per-brand membership + identifiers (Phase 3).
 * Distinct from auth (admin/customer/terminal credentials), which lives in AuthModule.
 */
@Module({})
export class IdentityModule {}
